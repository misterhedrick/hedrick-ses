import styles from '../styles/AddClientForm.module.scss';

const AddClientForm = (props) => {
  return (
    <form>
      <label for="name">Name</label>
      <input id="name"></input>
    </form>
  );
};
export default AddClientForm;
