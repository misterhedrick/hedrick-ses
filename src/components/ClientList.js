import Client from "./Client";
import Table from "./UI/Table";


const ClientList = (props) => {
  return (
    <Table>
      <Client />
      <Client />
      <Client />
      <Client />
      <Client />
    </Table>
  );
};

export default ClientList;
