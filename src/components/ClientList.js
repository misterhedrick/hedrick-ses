import Client from "./Client";
import Card from "./UI/Card";


const ClientList = (props) => {
  return (
    <Card>
      <Client />
      <Client />
      <Client />
      <Client />
      <Client />
    </Card>
  );
};

export default ClientList;
